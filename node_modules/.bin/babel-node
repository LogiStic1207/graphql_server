#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
<<<<<<< HEAD
  exec "$basedir/node"  "$basedir/../@babel/node/bin/babel-node.js" "$@"
else 
  exec node  "$basedir/../@babel/node/bin/babel-node.js" "$@"
=======
  "$basedir/node"  "$basedir/../babel-cli/bin/babel-node.js" "$@"
  ret=$?
else 
  node  "$basedir/../babel-cli/bin/babel-node.js" "$@"
  ret=$?
>>>>>>> parent of 4267e61 (여기서 flutter로 exported.json 데이터 보냄, exported.json 최신화 필요)
fi
exit $ret
